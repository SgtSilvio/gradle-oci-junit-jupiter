[versions]
junit-platform = "1.9.3"
oci-registry = "0.1.0"
testcontainers = "1.18.3"

[libraries]
junit-platform-launcher = { module = "org.junit.platform:junit-platform-launcher", version.ref = "junit-platform" }
oci-registry = { module = "io.github.sgtsilvio:oci-registry", version.ref = "oci-registry" }
testcontainers = { module = "org.testcontainers:testcontainers", version.ref = "testcontainers" }

[plugins]
kotlin-jvm = { id = "org.jetbrains.kotlin.jvm", version = "1.8.21" }
defaults = { id = "io.github.sgtsilvio.gradle.defaults", version = "0.2.0" }
metadata = { id = "com.github.sgtsilvio.gradle.metadata", version = "0.4.0" }
nexusPublish = { id = "io.github.gradle-nexus.publish-plugin", version = "1.3.0" }
